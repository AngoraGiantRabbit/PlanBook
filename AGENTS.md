# PlanBook

鸿蒙（卓易通）周视图计划本 App。领域语言见 `CONTEXT.md`，架构决策见 `docs/adr/`。

## Agent skills

### Issue tracker

Issues 跟踪在本仓库的 GitHub Issues（AngoraGiantRabbit/PlanBook），用 `gh` CLI 操作。见 `docs/agents/issue-tracker.md`。

### Triage labels

五个 triage 标签使用默认规范名（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文仓库：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
