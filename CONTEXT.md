# 计划本 App

一个面向鸿蒙系统（通过卓易通运行 Android 应用）的周视图计划本，采用「主计划本 + 子计划本」两层结构管理日程，支持四类任务、ICS 课程表导入、桌面小部件，以及自动生成的每日/每周/每月复盘待办。

## Language

**主计划本（Master Notebook）**：
App 全局唯一的顶层容器，由若干子计划本构成。**复盘、复盘设置和自动复盘待办都挂在主计划本上**——复盘是对整天的总结，不按子计划本分裂。

**子计划本（Sub-notebook）**：
主计划本内一个命名的任务集合，有自己的颜色。所有日程（任务）都落到子计划本上管理。每个子计划本有两个独立状态：
- **显示开关**：是否在计划本页（周视图）聚合显示。只影响显示，不影响数据。
- **活动状态**：同一时刻只有一个子计划本是活动的。

**活动子计划本（Active Sub-notebook）**：
当前写操作的目标——在首页新建任务、点空白时段加日程等操作都落到它上面。可在首页的子计划本 chips 或设置页的计划本管理中切换。切换活动子计划本不改变显示开关。

**导入子计划本（Imported Sub-notebook）**：
由 ICS 文件导入生成的子计划本（如课程表），内容是源文件在导入时刻的**只读快照**：其中的任务不可在 App 内编辑；重复导入同一课程表时整本删除重建（完成态随之丢弃）。

**计划本页（PlanBook Page）**：
周视图主页面，聚合显示所有「显示开关」打开的子计划本的任务，任务块按所属子计划本的颜色着色。

**任务（Task）**：
计划本内的一项待办事项，分为灵活、临时、每日、长期四种类型。

**灵活任务（Flex Task）**：
单天、无具体时段的任务。只设一个日期，落在周视图底部的灵活待办区域。

**临时任务（One-off Task）**：
单天、不重复的任务。只设一个日期，可选精确到分钟的开始/结束时间。

**每日任务（Daily Task）**：
按重复规则发生的任务，例如每天或每周指定几天，可选每天的开始/结束时间。设定一个起止区间，重复规则在该区间内生效。

**长期任务（Long-term Task）**：
跨多天的目标性任务，由「开始日 + 截止日期（DDL）」界定活跃区间，需要在复盘中逐步细化为灵活或临时任务。

**活跃区间（Active Range）**：
决定一项任务在某个日期是否可见的区间：
- 灵活/临时：单天，区间就是那一天（`startDate == endDate`）。
- 每日：`[startDate .. endDate]`，且仅当该日满足重复规则时可见。
- 长期：`[startDate .. DDL]`，仅当查看日落在区间内时才在周视图和复盘页显示；DDL 之后自动完成并从所有视图消失。

**复盘（Review）**：
用户针对某一天撰写的总结文本。每日复盘、每周复盘、每月复盘只是三个不同入口，最终都指向对应日期的复盘文本。

**自动待办（Auto Task）**：
系统根据主计划本的复盘设置自动生成的待办任务，作为普通任务显示在周视图中，点击后进入对应日期的复盘编辑页面。关闭某类复盘的开关时，已生成但未完成的该类自动待办随即删除；已完成的保留作历史。重新打开后按查看的周重新生成。

**完成（Completion）**：
一条任务的完成态只有两种归属，取决于它是否周期性发生：
- **按整体完成**（单实例任务）：ONE_OFF、FLEX、LONG_TERM。它们本质是「一件事」，勾掉即完成。
- **按天完成**（周期实例任务）：只有 DAILY。它按重复规则每天发生，周一做完不代表周二也做了，因此每天的完成态独立记录。

判断规则只有一条：`type == DAILY → 按天记录；否则 → 整体记录`。不得再用「是否跨多天」等临时条件为同类任务分裂出第二套完成逻辑。

**完成态的如实反映**：
DAILY 任务不自动标记逾期完成。过去某天是否完成，完全取决于用户那天是否勾选——没勾就是未完成，如实显示。不存在「DAILY 过期自动算完成」的语义。

**长期任务的过期自动完成**：
长期任务一旦越过 DDL 即视为放弃/过期，由系统自动标记完成（`isCompleted = true`）并从所有视图消失，不可在 UI 内找回。这与 DAILY「如实反映、不自动完成」是**有意的不对称**：长期目标过期意味着不再被跟进，而每日习惯某天没做只是那天的记录，语义不同。触发方式为查询时惰性标记（打开 App 或刷新时执行 `UPDATE ... WHERE type='LONG_TERM' AND endDate < today AND isCompleted=0`），不依赖后台定时任务。

## Relationships

- 一个 **App** 恰好包含一个 **主计划本**。
- 一个 **主计划本** 包含多个 **子计划本**、多个 **复盘** 和一套复盘设置。
- 一个 **任务** 必属于一个 **子计划本**，且只能是灵活、临时、每日、长期中的一种。
- 计划本页聚合显示所有显示开关打开的 **子计划本** 的任务；写操作落到 **活动子计划本**。
- 一个 **导入子计划本** 由 ICS 文件生成，其任务是只读快照，重导时整本重建。
- 一个 **长期任务** 可以在复盘中被细化为 **灵活任务** 或 **临时任务**，原长期任务仍然保留。
- 每个 **复盘** 对应一个具体日期；每周复盘和每月复盘分别打开周日和每月最后一天的复盘文本。
- 一个 **日期的复盘编辑页** 同时显示当天的复盘文本和当天已完成的任务。
- 一项任务只在查看日落在其 **活跃区间** 内时才显示；长期任务的活跃区间为 `[startDate .. DDL]`，过期后自动完成消失。

## Example dialogue

> **Dev:** “我在首页加一个任务，它会进哪个子计划本？”
> **Domain expert:** “进当前的活动子计划本——首页顶部那排 chips 里高亮的那枚。想换目标就先点一下别的子计划本再添加。”

> **Dev:** “如果我关掉‘健身’子计划本的显示开关，它的任务还在吗？”
> **Domain expert:** “在。显示开关只影响计划本页是否聚合显示它，数据不动；把开关再打开就回来了。”

> **Dev:** “导入的课程表能在 App 里改吗？比如把某节课的时间挪一下？”
> **Domain expert:** “不能。导入子计划本是源文件的只读快照，防止改了之后和源文件对不上。课表变了就重新导出 ics 再导入，整本重建。”

> **Dev:** “复盘待办属于哪个子计划本？”
> **Domain expert:** “不属于任何子计划本。复盘是‘对这一天’的总结，挂在主计划本上，和子计划本的划分无关。”

> **Dev:** “长期任务会显示在周视图主区域里吗？”
> **Domain expert:** “不会。长期任务有开始日和 DDL 但不占具体时段，它显示在底部独立的长期待办条里。只有查看日落在 `[开始日 .. DDL]` 区间内才显示；DDL 一过就自动完成、从所有视图消失。”

> **Dev:** “灵活和临时任务能跨多天吗？”
> **Domain expert:** “不能。灵活和临时都是单天的，只设一个日期。需要跨多天的目标用长期任务。”

> **Dev:** “每周复盘和每月复盘会创建新的文本吗？”
> **Domain expert:** “不会。它们只是特殊入口，最终都打开对应日期的那一份复盘文本。周日写周复盘、月底写月复盘，本质上还是在写当天的日记。”

> **Dev:** “复盘页面里能看到当天完成了哪些任务吗？”
> **Domain expert:** “能。点月历上的某一天进去，既能写复盘，也能看到那一天所有已完成的任务，包括时段上的临时和每日任务。”

## Flagged ambiguities

- 用户最初用“计划本”指代顶层容器，已明确为“独立任务集（Notebook）”。
- 用户最初将任务分为“临时、每日、长期”三类，后续补充了“灵活”作为第四类，专指无具体时段的任务。
- 灵活/临时任务最初允许跨多天（起止日期可不同），后明确为**单天**：`startDate == endDate`，UI 只暴露一个日期入口。跨多天的需求由长期任务承担。
- 长期任务最初“只设 DDL”且在固定底部条永远显示，后明确为「开始日 + DDL」界定活跃区间，且仅当查看日落在区间内才显示。
- 长期任务过 DDL 的处理曾讨论「保留可见」「只在待办列表可见」「自动完成消失」，最终明确为**自动完成并从所有视图消失**，与 DAILY「如实反映不自动完成」形成有意的不对称。
- 「计划本」最初是多个平级的独立任务集（可切换/合并），2026-09 重构为「**主计划本 + 子计划本**」两层：App 全局唯一主计划本，日程全落子计划本，多计划本切换/合并退役，由子计划本的显示开关替代切换场景。曾讨论多主计划本、平级聚合视图，均因层级过深或语义含糊否决（ADR-0004）。
- ICS 导入最初考虑映射为 DAILY（按 weeklyDays 重复），因无法表达单双周/调课/EXDATE 否决，定为**逐次展开为单天 ONE_OFF**；导入子计划本只读、重导整本重建（ADR-0005）。
- 复盘开关的语义最初只防新增不删旧（导致关闭后首页仍显示已生成的复盘待办），后明确关闭即**删除未完成的自动待办**、已完成保留作历史；复盘时段设置行改为始终可见（原来只在开关打开时渲染，让用户误以为不能设置时间）。
- 桌面小部件第一版定为**只读**（周视图网格+子计划本颜色+点按打开+手动刷新），勾选交互因 Glance 状态回写在鸿蒙/卓易通环境风险高而留待二期。
