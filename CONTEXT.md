# 计划本 App

一个面向鸿蒙系统（通过卓易通运行 Android 应用）的周视图计划本，支持多计划本管理、四类任务、以及自动生成的每日/每周/每月复盘待办。

## Language

**计划本（Notebook）**：
一个命名的独立任务集合。每个计划本拥有自己的任务、复盘和设置，同一时刻桌面小部件只展示一个当前计划本。

**当前计划本（Current Notebook）**：
用户当前在桌面小部件或主界面中查看的计划本。

**任务（Task）**：
计划本内的一项待办事项，分为灵活、临时、每日、长期四种类型。

**灵活任务（Flex Task）**：
只设置起止日期、不设置具体时段的任务，显示在周视图底部的灵活待办区域。

**临时任务（One-off Task）**：
不重复的任务，必须设置开始和结束日期，可选精确到分钟的开始/结束时间。

**每日任务（Daily Task）**：
按重复规则发生的任务，例如每天或每周指定几天，可选每天的开始/结束时间。

**长期任务（Long-term Task）**：
只设置截止日期（DDL）的任务，需要在每日/每周/每月复盘中逐步细化为灵活或临时任务。

**复盘（Review）**：
用户针对某一天撰写的总结文本。每日复盘、每周复盘、每月复盘只是三个不同入口，最终都指向对应日期的复盘文本。

**自动待办（Auto Task）**：
系统根据用户设置的复盘时间自动生成的待办任务，作为普通任务显示在周视图中，点击后进入对应日期的复盘编辑页面。

**完成（Completion）**：
一条任务的完成态只有两种归属，取决于它是否周期性发生：
- **按整体完成**（单实例任务）：ONE_OFF、FLEX、LONG_TERM。它们本质是「一件事」，勾掉即完成。跨多天的临时/灵活任务在每天列都显示，但共享同一个完成态——在任一天勾掉，所有天都标记完成。
- **按天完成**（周期实例任务）：只有 DAILY。它按重复规则每天发生，周一做完不代表周二也做了，因此每天的完成态独立记录。

判断规则只有一条：`type == DAILY → 按天记录；否则 → 整体记录`。不得再用「是否跨多天」等临时条件为同类任务分裂出第二套完成逻辑。

**完成态的如实反映**：
DAILY 任务不自动标记逾期完成。过去某天是否完成，完全取决于用户那天是否勾选——没勾就是未完成，如实显示。不存在「DAILY 过期自动算完成」的语义。

## Relationships

- 一个 **App** 包含多个 **计划本**。
- 一个 **计划本** 包含多个 **任务** 和多个 **复盘**。
- 一个 **任务** 必属于一个 **计划本**，且只能是灵活、临时、每日、长期中的一种。
- 一个 **长期任务** 可以在复盘中被细化为 **灵活任务** 或 **临时任务**，原长期任务仍然保留。
- 每个 **计划本** 有一个独立的 **当前计划本** 状态，用于决定桌面小部件显示哪个计划本。
- 每个 **复盘** 对应一个具体日期；每周复盘和每月复盘分别打开周日和每月最后一天的复盘文本。
- 一个 **日期的复盘编辑页** 同时显示当天的复盘文本和当天已完成的任务。

## Example dialogue

> **Dev:** “如果用户在桌面上完成了一个灵活任务，任务的状态会同步回计划本吗？”
> **Domain expert:** “会。桌面小部件只是当前计划本的视图，所有操作都直接反映在计划本的任务状态上。”

> **Dev:** “长期任务会显示在周视图主区域里吗？”
> **Domain expert:** “不会。长期任务只有 DDL，它不会占用具体时段，默认显示在底部灵活区域，提醒用户在复盘时细化它。”

> **Dev:** “每周复盘和每月复盘会创建新的文本吗？”
> **Domain expert:** “不会。它们只是特殊入口，最终都打开对应日期的那一份复盘文本。周日写周复盘、月底写月复盘，本质上还是在写当天的日记。”

> **Dev:** “复盘页面里能看到当天完成了哪些任务吗？”
> **Domain expert:** “能。点月历上的某一天进去，既能写复盘，也能看到那一天所有已完成的任务，包括时段上的临时和每日任务。”

## Flagged ambiguities

- 用户最初用“计划本”指代顶层容器，已明确为“独立任务集（Notebook）”。
- 用户最初将任务分为“临时、每日、长期”三类，后续补充了“灵活”作为第四类，专指无具体时段的任务。
