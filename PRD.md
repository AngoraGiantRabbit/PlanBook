# 计划本产品需求文档（PRD）

## 1. 产品概述

### 1.1 产品定位
一款面向华为鸿蒙系统（通过卓易通运行 Android 应用）的周视图计划本 App。核心体验围绕“桌面小部件 = 主页面”设计，强调快速查看、快速完成、快速复盘。

### 1.2 目标用户
需要管理日常学习/工作计划，并希望通过复盘持续迭代自己的个人用户。

### 1.3 核心设计理念
- **一页桌面即主页面**：桌面小部件占满一页，展示计划本页的周视图和灵活待办。
- **复盘即待办**：每日/每周/每月复盘不是隐藏功能，而是自动出现在计划中的普通任务，点击即可写。
- **长期任务逐步落地**：大型目标只设 DDL，通过在复盘中提醒和拆分，逐步变成可执行的具体任务。
- **复盘即历史**：每一天的复盘页面同时展示当天完成的全部任务，作为个人记录的入口。

## 2. 竞品调研与借鉴

### 2.1 常见计划本 App 形式

| App | 核心形态 | 优点 | 不足 |
|---|---|---|---|
| **滴答清单** | 列表 + 日历 + 番茄钟 | 功能全面、跨平台同步 | 功能过多，周视图弱，复盘不突出 |
| **Notion 模板** | 自由数据库页面 | 高度自定义 | 启动慢、手机端编辑重、无桌面小部件 |
| **Google Calendar** | 日历视图为主 | 时间管理直观 | 待办管理弱，无复盘和任务细化 |
| **Todoist** | 列表 + 标签 + 过滤器 | 简洁、自然语言输入 | 周视图弱，任务类型单一 |
| **手账类 App（如 Muji 日程本）** | 模拟纸质周计划 | 视觉舒适 | 无提醒、无自动化、难搜索 |
| **课表类 App（如超级课程表）** | 周视图网格 | 时段展示清晰 | 只适合做固定课程，不适合灵活待办 |

### 2.2 我们可以汲取的设计
- **滴答清单**：重复任务规则、完成状态、通知提醒（本阶段先不做通知）。
- **Google Calendar / 课表 App**：周视图网格清晰展示时段冲突。
- **手账 App**：一页看全周 + 底部随笔待办的布局节奏。
- **Notion 复盘模板**：日复盘/周复盘/月复盘三级结构，但我们要让它更轻量。

### 2.3 差异化亮点
1. **桌面小部件即主入口**：用户 80% 的使用场景不需要打开 App。
2. **复盘自动作为任务出现**：降低复盘被遗忘的概率。
3. **长期任务在复盘中被细化**：把“大目标拆解”这个行为嵌入到固定流程里。
4. **主计划本 + 子计划本**：日程分集合管理（如课程 / 健身 / 考研），按颜色区分聚合显示，显示开关表达"只看某一场景"。
5. **复盘页即当日历史**：写复盘时顺便回顾今天完成了什么。

## 3. 核心概念

详见 [CONTEXT.md](./CONTEXT.md)。关键术语：计划本、灵活任务、临时任务、每日任务、长期任务、复盘、自动待办。

## 4. 功能模块

### 4.1 主计划本与子计划本

App 全局唯一一个主计划本（ADR-0004），由若干子计划本构成；复盘、复盘设置、自动复盘待办挂主计划本。旧版"多计划本切换/合并"已退役，由子计划本显示开关替代。

#### 4.1.1 子计划本管理（设置页 → 计划本管理）
- 新建子计划本：命名 + 从预设调色板自动分配颜色（可改）。
- 显示开关：勾选哪些子计划本在计划本页聚合显示。只影响显示，数据不动。
- 活动子计划本：指定当前写操作（新建任务、点空白时段加日程）落到哪个子计划本；同一时刻仅一个。
- 重命名 / 删除子计划本（删除需确认，其任务一并删除）。

#### 4.1.2 首页子计划本 chips
- 计划本页顶部一排 chips，显示"显示开关"打开的子计划本各一枚（带各自颜色）。
- 点按 chip 切换活动子计划本（高亮态）；高频换写入目标不必进设置页。

#### 4.1.3 颜色
- 每个子计划本一种颜色；周视图任务块、chips、桌面小部件统一按所属子计划本着色。
- 调色板自动轮转分配，设置页可手选覆盖。

### 4.1.x ICS 导入（子计划本）

- 入口：设置页 → 计划本管理 → 导入 ICS。
- 行为：选择 .ics 文件（课程表），生成一个**导入子计划本**（ADR-0005）：
  - 每个 VEVENT 的 RRULE 在其区间内逐次展开为单天临时任务（含单双周、调课、EXDATE）。
  - 课名 → 任务名；教室 + 老师 → 备注，任务块内小字显示。
  - 默认打开显示开关、不抢占活动状态。
- 只读快照：导入子计划本的任务不可在 App 内编辑；重复导入 = 删旧整本重建（完成态丢弃）。

### 4.2 周视图主页面

#### 4.2.1 布局
- **顶部**：子计划本 chips（切换活动子计划本）+ 刷新按钮。
- **中部**：一周七天（周一到周日）的网格视图，聚合显示所有显示开关打开的子计划本的任务。
  - 横向为星期，纵向为时间（默认 06:00–24:00，可滚动）。
  - 有时段的临时任务和每日任务以彩色块显示在对应时段，块的颜色 = 所属子计划本的颜色；课表等带备注的任务在课名下方显示一行小字备注（教室 · 老师）。
- **底部**：两个独立的待办条，均按顶部高亮的「选中日」过滤。
  - **灵活待办区**：当天无具体时段的任务，包括灵活任务、未设时段的自动待办。
  - **长期待办区**：查看日落在 `[开始日 .. DDL]` 区间内的长期任务，独立成条（与灵活待办区语义不同：长期目标 ≠ 今日待办）。DDL 一过即自动完成、从此区消失。
  - 每个任务左侧有复选框，点击切换完成状态。
  - 已完成任务：显示删除线、灰色、移到最底部，再次点击可取消完成。

#### 4.2.2 交互
- 点击主区域任务块 → 打开任务详情/编辑页。
- 点击灵活任务文字/复选框 → 切换完成状态。
- 点击空白时段 → 弹出新建临时任务对话框，预填该时段。
- 点击右下角“+” → 弹出新建任务弹窗，选择任务类型。
- 点击顶部刷新按钮 → 重新从数据库加载并刷新视图。

### 4.3 任务管理

#### 4.3.1 任务类型

| 类型 | 必填字段 | 可选字段 | 在周视图中的显示 |
|---|---|---|---|
| 灵活任务 | 日期（单天） | 无 | 底部灵活区域 |
| 临时任务 | 日期（单天） | 开始时间、结束时间 | 有时段：主区域；无时段：底部灵活区域 |
| 每日任务 | 重复规则（每天/每周指定几天）、开始日期、结束日期 | 开始时间、结束时间 | 按规则展开到对应日期；有时段：主区域；无时段：底部灵活区域 |
| 长期任务 | 开始日、截止日期（DDL） | 无 | 底部独立的长期待办条，仅当查看日落在 `[开始日 .. DDL]` 内时显示；DDL 后自动完成消失 |

#### 4.3.2 每日任务重复规则
- 每天。
- 每周指定几天（可多选周一到周日）。
- 规则生效范围：开始日期到结束日期。

#### 4.3.3 任务操作
- 新增、编辑、删除。
- 标记完成/取消完成。
- 临时任务和每日任务可以“去掉时间”变为灵活任务。

### 4.4 复盘模块

#### 4.4.1 自动待办
- 系统为主计划本自动生成三类复盘待办：
  - **每日复盘**：每天晚上 21:00–21:30（默认，可编辑）。
  - **每周复盘**：每周日晚上 21:00–21:30（默认，可编辑）。
  - **每月复盘**：每月最后一天晚上 21:00–21:30（默认，可编辑）。
- 这些待办作为普通任务显示在周视图中，属于临时任务类型，不挂在任何子计划本上。
- 标题示例：“📝 每日复盘”“📝 每周复盘”“📝 每月复盘”。
- 点击后进入对应日期的复盘编辑页。

#### 4.4.2 复盘时间设置
- 入口：设置页 / 复盘 Tab。
- 可分别设置三类复盘的时间（开始时间、结束时间）。
- 可开关是否生成对应复盘待办。
- **开关语义**：关闭某类复盘时，已生成但未完成的该类自动待办随即删除（已完成的历史保留）；重新打开后按查看的周重新生成。
- **时段设置行始终显示**（不论开关状态），避免关闭后找不到时间入口。

#### 4.4.3 复盘日历
- 独立的底部 Tab“复盘”。
- 显示月历，默认定位到今天所在月。
- 今天高亮显示；有复盘内容的日期下方加小圆点。
- 左右滑动切换月份。
- 点击某天 → 打开复盘编辑页。

#### 4.4.4 复盘编辑页
- 顶部显示日期。
- 上方为“当日已完成任务”区域：
  - 列出该日期内所有已完成的任务，包括主区域里的临时/每日任务和底部灵活区域的任务。
  - 任务按完成时间排序，无完成时间则按任务类型排序。
- 下方为大段文本输入框。
- 自动保存：停止输入 1 秒后保存到本地。
- 页面底部“待细化长期任务”区域：
  - 列出 DDL 在当前日期之前或临近的长期任务。
  - 点击长期任务 → 弹出创建临时/灵活任务对话框，预填标题。
  - 原长期任务保留。

## 5. 桌面小部件

### 5.1 尺寸
- 默认占满一页（根据设备屏幕网格，例如 4×6 或 5×7）。
- 用户可手动调整大小，布局自适应。
- 最小建议尺寸：4×4。

### 5.2 显示内容（第一版：只读）
- 与计划本页同源：当前一周的日程网格，聚合显示所有显示开关打开的子计划本。
- 任务块按所属子计划本的颜色着色；带备注的任务显示小字备注。
- 底部灵活待办区（只读展示选中日/今天的灵活与长期待办）。

### 5.3 交互（第一版：只读）
- 点击任意区域（任务块/空白）→ 打开 App 主页面。
- 小部件刷新采用手动刷新机制，点击 App 内的刷新按钮后同步更新小部件。
- 勾选完成等写交互**留待二期**：Glance 状态回写在鸿蒙/卓易通环境风险高，先保证显示正确。

## 6. 数据与存储

### 6.1 存储方式
- 纯本地存储，使用 Android Room 数据库。
- 不联网、不登录、不自动同步。

### 6.2 未来可扩展
- 导出/导入 JSON 备份。
- 云端同步（后续版本再评估）。

## 7.  MVP 范围

### 7.1 已实现（第一版）
1. ~~多计划本：新建、切换、重命名、合并。~~（已被主/子计划本两层模型取代，见 7.1x）
2. 四类任务：灵活、临时、每日（每天/每周指定几天）、长期。
3. 周视图主页面：网格展示时段任务 + 底部灵活待办，时间范围 06:00–24:00。
4. 完成状态：打勾变灰、删除线、移到底部、可撤销。
5. 复盘：每日/每周/每月自动待办、复盘时间设置、月历、编辑页、自动保存。
6. 复盘页显示当天已完成的任务，包括主区域的临时/每日任务。
7. 长期任务在复盘页提示并可拆分为临时/灵活任务。
8. 桌面小部件：只读版——一周日程网格、子计划本着色、点按打开 App、手动刷新。

### 7.1x 下一阶段必须实现
1. 主/子计划本两层模型（ADR-0004）：迁移、设置页子计划本管理、首页 chips、按子计划本着色。
2. ICS 导入（ADR-0005）：解析、展开为单天临时任务、只读导入子计划本、重导整本重建。
3. 复盘开关语义修复：关闭即删未完成自动待办；时段设置行始终显示。
4. 桌面小部件只读版落地（注册 receiver + provider xml + 数据聚合渲染）。

### 7.2 本次不做
1. 消息通知/提醒。
2. 云端同步/账号系统。
3. 每月指定日期的重复规则。
4. 自定义重复周期。
5. 自动复盘待办标题/图标自定义。
6. 月历上区分“有复盘待办”和“已写复盘”两种状态。
7. 小部件勾选完成（二期）。
8. ICS 导出（只进不出）。

## 8. 已确认的设计决策

| 决策项 | 结论 |
|---|---|
| 计划本结构 | 单主计划本 + 子计划本两层（ADR-0004），多计划本切换/合并退役 |
| 写操作目标 | 活动子计划本，首页 chips + 设置页两处切换 |
| 复盘归属 | 主计划本（不按子计划本分裂） |
| 子计划本颜色 | 调色板自动轮转分配，设置页可改，任务块按所属子本着色 |
| ICS 导入 | RRULE 逐次展开为单天临时任务；导入子本只读，重导整本重建（ADR-0005） |
| 任务类型 | 灵活、临时、每日、长期 |
| 灵活/临时日期 | 单天，UI 一个日期入口，`startDate == endDate` |
| 每日日期 | 起止区间 + 重复规则，规则在区间内生效 |
| 长期任务日期 | 开始日 + DDL，仅当查看日落在 `[开始日 .. DDL]` 内才显示 |
| 长期任务过期 | DDL 后自动完成并从所有视图消失（不可找回） |
| 周视图时间范围 | 06:00–24:00 |
| 完成状态样式 | 删除线、灰色、移到最底部、可撤销 |
| 复盘入口 | 自动待办 + 月历，三类复盘都指向当天复盘文本 |
| 复盘开关语义 | 关闭即删未完成自动待办（已完成保留）；时段行始终显示 |
| 复盘页内容 | 当天复盘文本 + 当天已完成任务 + 待细化长期任务 |
| 桌面小部件 | 第一版只读（周网格+着色+点按打开+手动刷新），勾选留二期 |
| 数据存储 | 纯本地 Room 数据库 |
| 通知/云端 | 不做 |

## 9. 参考与启发

- 滴答清单：重复任务与完成状态设计。
- Google Calendar：周视图网格与时段冲突可视化。
- 手账 App：一页看全周 + 底部随笔的布局节奏。
- Notion 复盘模板：日复盘/周复盘/月复盘三级结构。
